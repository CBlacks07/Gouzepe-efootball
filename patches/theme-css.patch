diff --git a/theme.css b/theme.css
index 0000000..0000001 100644
--- a/theme.css
+++ b/theme.css
@@ -1,5 +1,17 @@
 /* Thème principal */
 :root{
   /* ... vos variables existantes ... */
 }

+/* Accessibilité & états globaux */
+:where(button,input,select,textarea):focus-visible{
+  outline: none;
+  /* Anneau subtil basé sur la couleur d'info si dispo (sinon reste discret) */
+  box-shadow: 0 0 0 3px color-mix(in srgb, var(--info-500, #60a5fa) 35%, transparent);
+}
+
+.btn:disabled, input:disabled, select:disabled, textarea:disabled{
+  opacity:.6;
+  cursor:not-allowed;
+}
